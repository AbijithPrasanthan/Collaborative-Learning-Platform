renderCards =
    () => {
        const topic =
            document.querySelector(
                ".newMeetingTopic"
            );
        const sub =
            document.querySelector(
                ".newMeetingSubject"
            );
        const time =
            document.querySelector(
                ".newMeetingTime"
            );

        console.log(
            topic,
            sub,
            time
        );
    };